/* dummy stubs */#include "SDL_stdinc.h"#include "SDL_atomic.h"#include <stdio.h>/* This is just a placeholder, obviously not really atomic at all */int _SDL_xchg_mwerks(SDL_SpinLock *lock,int v){    /*uint32_t oldintr;*/    bool res = 0;    /* disable interuption    oldintr = DIntr();*/    if (*lock == 0) {        *lock = 1;        res = 1;    }    /* enable interuption    if (oldintr) {        EIntr();    }*/    return res;}