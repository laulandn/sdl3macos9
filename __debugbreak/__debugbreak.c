#include <stdio.h>#include <stdlib.h>/* Not sure when this would get called, so we just exit... */void __debugbreak(){  fprintf(stderr,"__debugbreak called!\n");  fflush(stderr);  exit(EXIT_FAILURE);}