This includes some atomic func implementations for m68k which are by Apple only for PowerPC.

There is a stub for _SDL_xchg_macosclassic() which I don't think is needed anymore.

It is probably only needed for CodeWarrior.
